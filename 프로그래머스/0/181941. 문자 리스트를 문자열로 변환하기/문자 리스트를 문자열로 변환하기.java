class Solution {
    public String solution(String[] arr) {
        StringBuilder sb = new StringBuilder();
        
        for (String tmp : arr) {
            sb.append(tmp);
        }
        
        return sb.toString();
    }
}